{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Cajda123/hass/ai/schemas/event.schema.json",
  "title": "EMS AI Event",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "topic",
    "type",
    "key",
    "importance",
    "status_display",
    "reason_display",
    "human_hint",
    "recommendation_hint",
    "allowed_topics",
    "forbidden_topics",
    "notify",
    "speak",
    "silent"
  ],
  "properties": {
    "topic": {
      "type": "string",
      "description": "Source MQTT topic or logical event source, for example ems/safety."
    },
    "type": {
      "type": "string",
      "description": "Logical event type: safety, wallbox, boiler, battery_grid, plan, appliance."
    },
    "key": {
      "type": "string",
      "description": "Stable deduplication key for anti-spam logic."
    },
    "importance": {
      "type": "string",
      "enum": [
        "debug",
        "normal",
        "important",
        "critical"
      ],
      "default": "normal"
    },
    "status": { "type": "string" },
    "status_display": { "type": "string" },
    "reason": { "type": "string" },
    "reason_display": { "type": "string" },
    "human_hint": {
      "type": "string",
      "description": "Authoritative human-readable statement of what happened. The model should rewrite it, not reinterpret it."
    },
    "recommendation_hint": {
      "type": "string",
      "description": "Authoritative recommendation. If omitted, the model must not invent advice."
    },
    "allowed_topics": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Topics the output may mention."
    },
    "forbidden_topics": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Topics the output must not mention."
    },
    "silent": {
      "type": "boolean",
      "default": false,
      "description": "When true, the commentator must return BEZ_KOMENTARE."
    },
    "notify": {
      "type": "boolean",
      "description": "Whether this event should be considered for mobile notification."
    },
    "speak": {
      "type": "boolean",
      "description": "Whether this event may be spoken by a future voice layer."
    },
    "human_hint": {
      "type": "string",
      "description": "Short Czech explanation of what happened; source text for AI, not a command."
    },
    "recommendation_hint": {
      "type": "string",
      "description": "Allowed Czech recommendation; AI must not recommend actions outside this hint."
    },
    "allowed_topics": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Topics the AI may mention for this event."
    },
    "forbidden_topics": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Topics the AI must not mention for this event."
    },
    "silent": {
      "type": "boolean",
      "description": "Whether the automatic commentator must ignore this event."
    }
  }
}
